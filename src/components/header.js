function Header() {
return (
    <div className="jumbotron">
        <h1 className="display-4">Employee Directory</h1>
        <hr className="my-4"/>
        <p>Search for employees by first name, sort by first name, or sort by start date.</p>
    </div>
    );
}

export default Header;